package workers

func Expiration() {
	// services := entity.GetExpires()

	// var all []entity.Service
	// for userID, s := range services {
	// 	u, err := user.GetByID(userID)
	// 	assert.Nil(err)

	// 	notif := notification.NewNotification(notification.MailType)
	// 	notif.To = []string{u.Email}
	// 	notif.Body = mail.ParseTemplate(mail.WelcomeMail, struct {
	// 		Name string
	// 	}{u.Name})
	// 	mail.Send(notif)

	// 	all = append(all, s...)
	// }
	// service.SetStatus(all, service.Paused)
}
